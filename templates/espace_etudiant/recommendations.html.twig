{% extends 'base.html.twig' %}

{% block title %}Recommandations IA{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4"><i class="fas fa-robot"></i> Recommandations Personnalisées par IA</h1>
    
    <div class="alert alert-info mb-4">
        <h5 class="alert-heading"><i class="fas fa-info-circle"></i> À propos de ces recommandations</h5>
        <p>Notre système d'intelligence artificielle analyse vos performances académiques et vous propose des cours en ligne adaptés pour améliorer vos compétences dans les modules où vous rencontrez des difficultés.</p>
    </div>
    
    {% if recommendations|length > 0 %}
    <div class="row">
        {% for recommendation in recommendations %}
        <div class="col-md-12 mb-4">
            <div class="card border-{% if recommendation.priority == 'HIGH' %}danger{% elseif recommendation.priority == 'MEDIUM' %}warning{% else %}info{% endif %}">
                <div class="card-header bg-{% if recommendation.priority == 'HIGH' %}danger{% elseif recommendation.priority == 'MEDIUM' %}warning{% else %}info{% endif %} text-white">
                    <div class="d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">{{ recommendation.courseName }}</h5>
                        <span class="badge bg-light text-dark">Priorité: {{ recommendation.priority }}</span>
                    </div>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-4">
                            <h6>Performance Actuelle</h6>
                            <div class="text-center mb-3">
                                <div class="display-4 text-{% if recommendation.average < 8 %}danger{% elseif recommendation.average < 10 %}warning{% else %}info{% endif %}">
                                    {{ recommendation.average }}/20
                                </div>
                                <p class="text-muted">{{ recommendation.status }}</p>
                            </div>
                        </div>
                        <div class="col-md-8">
                            <h6><i class="fas fa-graduation-cap"></i> Cours Recommandés</h6>
                            <div class="list-group">
                                {% for courseName, courseUrl in recommendation.recommendedCourses %}
                                <a href="{{ courseUrl }}" target="_blank" class="list-group-item list-group-item-action">
                                    <div class="d-flex w-100 justify-content-between">
                                        <h6 class="mb-1">{{ courseName }}</h6>
                                        <small><i class="fas fa-external-link-alt"></i></small>
                                    </div>
                                    <small class="text-muted">{{ courseUrl }}</small>
                                </a>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                    
                    <div class="mt-3">
                        <h6><i class="fas fa-bullseye"></i> Objectif</h6>
                        <p class="text-muted">Améliorer votre moyenne à au moins 12/20 dans ce module en suivant les cours recommandés et en pratiquant régulièrement.</p>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
    
    <!-- Additional Tips -->
    <div class="card bg-light mt-4">
        <div class="card-body">
            <h5><i class="fas fa-lightbulb"></i> Conseils pour Améliorer vos Résultats</h5>
            <ul>
                <li>Suivez les cours recommandés de manière régulière et constante</li>
                <li>Pratiquez avec des exercices supplémentaires</li>
                <li>N'hésitez pas à demander de l'aide à vos professeurs</li>
                <li>Rejoignez des groupes d'étude avec vos camarades</li>
                <li>Fixez-vous des objectifs hebdomadaires réalisables</li>
            </ul>
        </div>
    </div>
    
    {% else %}
    <div class="alert alert-success" role="alert">
        <h4 class="alert-heading"><i class="fas fa-check-circle"></i> Félicitations!</h4>
        <p>Vous n'avez actuellement aucun cours nécessitant une amélioration. Continuez vos excellents efforts!</p>
        <hr>
        <p class="mb-0">Maintenez votre niveau en révisant régulièrement et en restant engagé dans vos études.</p>
    </div>
    {% endif %}
    
    <div class="mt-4">
        <a href="{{ path('app_espace_etudiant_dashboard') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Retour au tableau de bord
        </a>
        <a href="{{ path('app_espace_etudiant_results') }}" class="btn btn-primary">
            <i class="fas fa-chart-bar"></i> Voir mes résultats
        </a>
    </div>
</div>
{% endblock %}
