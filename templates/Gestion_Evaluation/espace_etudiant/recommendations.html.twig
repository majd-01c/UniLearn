{% extends 'base.html.twig' %}

{% block title %}Recommandations IA â€” Groq{% endblock %}

{% block body %}
<div class="container-fluid mt-4 px-4 animate-in">

    {# â”€â”€ Header â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ #}
    <div class="uni-page-header">
        <div class="uni-page-header-info">
            <h1 class="uni-page-header-title">
                <i class="bi bi-robot"></i> Recommandations IA PersonnalisÃ©es
            </h1>
            <p class="uni-page-header-subtitle">AnalysÃ©es par Groq Â· LLaMA 3.3 70B Â· en temps rÃ©el</p>
        </div>
        <div class="uni-page-header-actions">
            <a href="{{ path('app_espace_etudiant_results') }}" class="btn btn-primary btn-sm">
                <i class="bi bi-bar-chart-fill"></i> Mes RÃ©sultats
            </a>
        </div>
    </div>

    {# â”€â”€ AI source badge â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ #}
    {% if recommendations|length > 0 and recommendations[0].fromGroq is defined and recommendations[0].fromGroq %}
    <div class="alert border-0 mb-4 d-flex align-items-center gap-3"
         style="background:linear-gradient(135deg,#f8f0ff 0%,#eff5ff 100%);border-left:4px solid #7c3aed!important;">
        <span style="font-size:2rem;">ðŸ¤–</span>
        <div>
            <strong style="color:#7c3aed;">Powered by Groq Â· LLaMA 3.3 70B</strong>
            <p class="mb-0 small text-muted">Ces recommandations ont Ã©tÃ© gÃ©nÃ©rÃ©es en temps rÃ©el par une vraie IA, personnalisÃ©es selon vos notes rÃ©elles dans chaque module.</p>
        </div>
    </div>
    {% else %}
    <div class="alert alert-info border-primary mb-4" style="border-left:5px solid #0d6efd;">
        <i class="bi bi-info-circle-fill me-2"></i>
        <strong>Mode hors-ligne :</strong> recommandations gÃ©nÃ©rÃ©es par rÃ¨gles. Configurez <code>GROQ_API_KEY</code> dans le fichier <code>.env</code> pour activer l'IA rÃ©elle.
    </div>
    {% endif %}

    {% if recommendations|length > 0 %}
    <div class="row g-4 stagger-in">
        {% for rec in recommendations %}
        {% set pColor = rec.priority == 'HIGH' ? 'danger' : (rec.priority == 'MEDIUM' ? 'warning' : 'info') %}
        {% set pIcon  = rec.priority == 'HIGH' ? 'exclamation-triangle-fill' : (rec.priority == 'MEDIUM' ? 'dash-circle-fill' : 'info-circle-fill') %}

        <div class="col-12">
            <div class="card shadow-sm" style="border-left:5px solid var(--bs-{{ pColor }});">

                {# Header #}
                <div class="card-header bg-{{ pColor }} bg-opacity-10 d-flex justify-content-between align-items-center flex-wrap gap-2">
                    <h5 class="mb-0 fw-bold">
                        <i class="bi bi-book-fill me-2"></i>{{ rec.courseName }}
                    </h5>
                    <span class="badge bg-{{ pColor }}">
                        <i class="bi bi-{{ pIcon }} me-1"></i>PrioritÃ© {{ rec.priority }}
                    </span>
                </div>

                <div class="card-body">
                    <div class="row g-4">

                        {# â”€â”€ Score + Insight â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ #}
                        <div class="col-md-3 d-flex flex-column align-items-center text-center border-end">
                            <p class="text-muted small mb-1 fw-semibold text-uppercase">Moyenne actuelle</p>
                            <div class="display-4 fw-bold text-{{ rec.average < 8 ? 'danger' : (rec.average < 10 ? 'warning' : 'info') }}">
                                {{ rec.average }}/20
                            </div>
                            <span class="badge bg-{{ rec.average >= 10 ? 'success' : 'danger' }} mt-2">{{ rec.status }}</span>

                            {% if rec.aiInsight %}
                            <div class="alert alert-light border mt-3 text-start w-100" style="font-size:.83rem;">
                                <i class="bi bi-lightbulb-fill text-warning me-1"></i>
                                <strong>Analyse :</strong><br>{{ rec.aiInsight }}
                            </div>
                            {% endif %}
                        </div>

                        {# â”€â”€ Platforms + YouTube â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ #}
                        <div class="col-md-9">

                            {# Platforms #}
                            {% if rec.platforms is defined and rec.platforms|length > 0 %}
                            <h6 class="fw-bold mb-3 text-muted">
                                <i class="bi bi-globe2 me-1 text-primary"></i>Plateformes recommandÃ©es
                            </h6>
                            <div class="row g-2 mb-4">
                                {% for pl in rec.platforms %}
                                <div class="col-sm-6 col-xl-4">
                                    <a href="{{ pl.url }}" target="_blank" rel="noopener"
                                       class="d-flex align-items-start gap-2 p-3 border rounded text-decoration-none hover-lift h-100"
                                       style="background:#fafafa;">
                                        {% set ico = pl.icon ?? 'web' %}
                                        {% if ico == 'youtube' %}<i class="bi bi-youtube text-danger fs-4 flex-shrink-0"></i>
                                        {% elseif ico == 'khan' %}<span class="flex-shrink-0 fs-4">ðŸŽ“</span>
                                        {% elseif ico == 'coursera' %}<span class="flex-shrink-0 fs-4">ðŸ“˜</span>
                                        {% elseif ico == 'udemy' %}<span class="flex-shrink-0 fs-4">ðŸŽ¯</span>
                                        {% elseif ico == 'openclassrooms' %}<span class="flex-shrink-0 fs-4">ðŸ«</span>
                                        {% elseif ico == 'edx' %}<span class="flex-shrink-0 fs-4">ðŸŽ“</span>
                                        {% elseif ico == 'mit' %}<span class="flex-shrink-0 fs-4">ðŸ›ï¸</span>
                                        {% else %}<i class="bi bi-link-45deg text-primary fs-4 flex-shrink-0"></i>
                                        {% endif %}
                                        <div>
                                            <div class="fw-semibold small">{{ pl.name }}</div>
                                            <div class="text-muted" style="font-size:.77rem;">{{ pl.description }}</div>
                                        </div>
                                    </a>
                                </div>
                                {% endfor %}
                            </div>
                            {% elseif rec.recommendedCourses is defined and rec.recommendedCourses|length > 0 %}
                            <h6 class="fw-bold mb-3 text-muted"><i class="bi bi-stars me-1 text-warning"></i>Cours recommandÃ©s</h6>
                            <div class="list-group mb-4">
                                {% for name, url in rec.recommendedCourses %}
                                <a href="{{ url }}" target="_blank" class="list-group-item list-group-item-action hover-lift d-flex justify-content-between align-items-center">
                                    <span><i class="bi bi-play-circle-fill text-primary me-2"></i>{{ name }}</span>
                                    <i class="bi bi-box-arrow-up-right text-muted"></i>
                                </a>
                                {% endfor %}
                            </div>
                            {% endif %}

                            {# YouTube searches #}
                            {% if rec.youtubeSearches is defined and rec.youtubeSearches|length > 0 %}
                            <h6 class="fw-bold mb-3 text-muted">
                                <i class="bi bi-youtube text-danger me-1"></i>Recherches YouTube suggÃ©rÃ©es
                            </h6>
                            <div class="d-flex flex-wrap gap-2 mb-3">
                                {% for yt in rec.youtubeSearches %}
                                <a href="{{ yt.url }}" target="_blank" rel="noopener"
                                   class="btn btn-outline-danger btn-sm d-flex align-items-center gap-1 hover-lift">
                                    <i class="bi bi-play-fill"></i>
                                    {{ yt.label }}
                                    <i class="bi bi-box-arrow-up-right" style="font-size:.65rem;opacity:.6;"></i>
                                </a>
                                {% endfor %}
                            </div>
                            {% endif %}

                            {# Goal box #}
                            <div class="mt-3 p-3 rounded" style="background:linear-gradient(135deg,#f0fdf4,#ecfdf5);">
                                <h6 class="mb-1 text-success"><i class="bi bi-bullseye me-1"></i>Objectif</h6>
                                <p class="mb-0 small text-muted">
                                    Atteindre <strong>12/20 minimum</strong> dans ce module.
                                    L'IA mettra Ã  jour ses recommandations Ã  chaque mise Ã  jour de vos notes.
                                </p>
                            </div>

                        </div>{# /col-md-9 #}
                    </div>{# /row #}
                </div>{# /card-body #}
            </div>{# /card #}
        </div>
        {% endfor %}
    </div>{# /row #}

    {# General tips #}
    <div class="card mt-4 border-success" style="border-left:5px solid #198754;">
        <div class="card-header bg-success bg-opacity-10">
            <h5 class="mb-0"><i class="bi bi-robot text-success me-2"></i>Conseils gÃ©nÃ©raux de l'IA</h5>
        </div>
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-6">
                    <div class="d-flex align-items-start">
                        <i class="bi bi-calendar-check-fill text-primary me-3 fs-4"></i>
                        <div><h6 class="fw-bold">RÃ©gularitÃ© (30â€“60 min/jour)</h6>
                        <small class="text-muted">Des sessions courtes et rÃ©guliÃ¨res sont bien plus efficaces que de longues sessions espacÃ©es.</small></div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="d-flex align-items-start">
                        <i class="bi bi-youtube text-danger me-3 fs-4"></i>
                        <div><h6 class="fw-bold">Regardez, puis pratiquez</h6>
                        <small class="text-muted">AprÃ¨s chaque vidÃ©o, faites immÃ©diatement des exercices. Ne restez pas passif.</small></div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="d-flex align-items-start">
                        <i class="bi bi-people-fill text-info me-3 fs-4"></i>
                        <div><h6 class="fw-bold">Posez des questions</h6>
                        <small class="text-muted">Utilisez le forum, les commentaires YouTube ou demandez directement Ã  votre enseignant.</small></div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="d-flex align-items-start">
                        <i class="bi bi-graph-up-arrow text-warning me-3 fs-4"></i>
                        <div><h6 class="fw-bold">Suivez vos progrÃ¨s</h6>
                        <small class="text-muted">Revenez sur cette page aprÃ¨s chaque Ã©valuation pour mesurer l'impact de votre travail.</small></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {% else %}

    <div class="card border-success" style="border-left:5px solid #198754;">
        <div class="card-body text-center py-5">
            <i class="bi bi-trophy-fill text-success" style="font-size:4rem;"></i>
            <h3 class="mt-4 fw-bold">FÃ©licitations ! ðŸŽ‰</h3>
            <p class="lead text-muted">Aucun module ne nÃ©cessite d'amÃ©lioration pour le moment.</p>
            <p class="text-muted">L'IA surveille continuellement vos notes et vous alertera dÃ¨s qu'un module demande votre attention.</p>
            <a href="{{ path('app_espace_etudiant_results') }}" class="btn btn-success mt-2">
                <i class="bi bi-bar-chart-fill me-1"></i>Voir mes rÃ©sultats
            </a>
        </div>
    </div>

    {% endif %}

    <div class="mt-4">
        <a href="{{ path('app_espace_etudiant_dashboard') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left me-1"></i>Retour au tableau de bord
        </a>
    </div>
</div>

<style>
.hover-lift { transition: transform .18s ease, box-shadow .18s ease; }
.hover-lift:hover { transform: translateY(-3px); box-shadow: 0 6px 18px rgba(0,0,0,.1); }
</style>
{% endblock %}
